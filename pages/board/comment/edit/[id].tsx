import { useRouter } from 'next/router';
import React from 'react';
import CommentEditScreen from 'src/app.features/board/screens/CommentEditScreen';

function EditComment() {
	/* const {
		query: { prevComment, ,id },
	} = useRouter(); */

	return <div />;
}

export default EditComment;
